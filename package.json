{
  "name": "swap-widget-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:site && npm run build:widget",
    "build:site": "vite build",
    "build:widget": "vite build --config vite.widget-runtime.config.js && vite build --config vite.widget.config.js && vite build --config vite.widget-test-runtime.config.js && vite build --config vite.widget-test.config.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@cowprotocol/widget-lib": "^2.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.19"
  }
}
